Rounded custom checkbox with a green fill + check glyph when checked.

```jsx
<Checkbox checked={optIn} onChange={e => setOptIn(e.target.checked)} label="Get delivery updates & exclusive offers on WhatsApp" />
```
